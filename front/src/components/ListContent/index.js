import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ListContent = () => {
    return (
        
        <View style={{marginTop: 60, borderLeftWidth:1, borderRadius:10, backgroundColor:'white', opacity:0.7}}>
          <Text style={{fontWeight:'bold', marginBottom:5,paddingHorizontal:5}}>aaaaaaaaaaaaaaaaaa</Text>
          <Text style={{fontWeight:'bold', marginBottom:5,paddingHorizontal:5}}>aaaaaaaaaaaaaaaaaa</Text>
          <Text style={{fontWeight:'bold', marginBottom:5,paddingHorizontal:5}}>aaaaaaaaaaaaaaaaaa</Text>
          <Text style={{fontWeight:'bold', marginBottom:5,paddingHorizontal:5}}>aaaaaaaaaaaaaaaaaa</Text>
        </View>
    )
}

export default ListContent

const styles = StyleSheet.create({})
